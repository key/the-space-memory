{
  "mcpServers": {
    "serena": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i", "--network", "host",
        "-v", ".:/workspaces/project",
        "ghcr.io/oraios/serena:latest",
        "serena", "start-mcp-server"
      ]
    }
  }
}
